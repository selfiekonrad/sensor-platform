apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-management-service
  labels:
    app: sensor-management-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sensor-management-service
  template:
    metadata:
      name: sensor-management-service
      labels:
        app: sensor-management-service
    spec:
      containers:
        - name: sensor-management-service
          image: ghcr.io/selfiekonrad/sensor-management-service:0.0.3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: DB_URL
              valueFrom:
                configMapKeyRef:
                  key: db_url
                  name: credentials-db
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  key: db_username
                  name: credentials-db
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  key: db_password
                  name: credentials-db
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: sensor-management-service
  labels:
    app: sensor-management-service
spec:
  type: ClusterIP
  selector:
    app: sensor-management-service
  ports:
    - name: http
      port: 81
      targetPort: 8081
      protocol: TCP
